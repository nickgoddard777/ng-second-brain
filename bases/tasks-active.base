views:
  - type: table
    name: Table
    filters:
      and:
        - file.path != this.file.path
        - type == "task"
        - '!status.contains("complete")'
    groupBy:
      property: project
      direction: ASC
    order:
      - project
      - file.name
      - status
      - tags
